#include <bits/stdc++.h>
using namespace std;

struct points
{
    int a, b;
};

int main()
{
    int N, M;
    cin >> N >> M;

    points inp[M];
    for (int i = 0; i < M; i++)
        cin >> inp[i].a >> inp[i].b;
    
    for (auto i : inp)
    {
        
    }

    return 0;
}
